package tiles.tileTypes;

import javafx.scene.image.Image;

public class ResourceTile extends GenericTile {

    ResourceTile(Image tileImage, String tileCategory) {
        super(tileImage, tileCategory);
    }
}
