package tiles.tileTypes;

import javafx.scene.image.Image;

public class BuildingTile extends GenericTile {
    BuildingTile(Image tileImage, String tileCategory) {
        super(tileImage, tileCategory);
    }
}
